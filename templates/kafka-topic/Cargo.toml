[package]
name = "{{project-name}}"
version = "0.1.0"
edition = "2024"

[dependencies]
ruststream = { version = "0.6", features = ["macros", "json", "asyncapi", "logging"] }
ruststream-rdkafka = "0.6"
schemars = "1"
serde = { version = "1", features = ["derive"] }
